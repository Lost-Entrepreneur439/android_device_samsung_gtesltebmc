BLUETOOTH_UART_DEVICE_PORT = "/dev/ttyS0"  /* Adjust if necessary */
FW_PATCHFILE_LOCATION = "/vendor/firmware"
FW_PATCH_SETTLEMENT_DELAY_MS = 100
UART_TARGET_BAUD_RATE = 115200  /* Check if a lower baud rate is needed */
LPM_IDLE_TIMEOUT_MULTIPLE = 5
SCO_USE_I2S_INTERFACE = TRUE  /* Adjust based on actual hardware */
SCO_I2SPCM_IF_ROLE = 0  /* Verify if this is the correct role */
